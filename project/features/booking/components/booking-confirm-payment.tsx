import { DetachedModalHeader } from "@/navigation/components/detached-modal-header";

export function BookingConfirmPayment() {
  return (
    <DetachedModalHeader showCloseButton={false} title="Confirmar y pagar" />
  );
}
