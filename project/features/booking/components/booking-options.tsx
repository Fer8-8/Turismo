import { DetachedModalHeader } from "@/navigation/components/detached-modal-header";

export function BookingOptions() {
  return (
    <DetachedModalHeader showCloseButton={false} title="Opciones de reserva" />
  );
}
