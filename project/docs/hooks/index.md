# Hooks documentation

Hooks in the `hooks` directory are custom hooks shared across the app. For feature-specific hooks, they should be placed in the `features` directory.

## Components
- [useUserLocation](./use-user-location)
