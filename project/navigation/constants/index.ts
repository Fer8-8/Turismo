/**
 * Velocity threshold for fast swipes (pixels per second)
 */
export const VELOCITY_THRESHOLD = 500;

/**
 * Scroll position threshold to consider "at top" (in pixels)
 */
export const SCROLL_TOP_THRESHOLD = 5;
